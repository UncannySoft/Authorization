@{
    ViewData["Title"] = "Manage Account";
}

<div>Hello @User.Identity.Name!</div>
<div>
    <dl>
        @foreach (var claim in User.Claims){
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    </dl>
</div>